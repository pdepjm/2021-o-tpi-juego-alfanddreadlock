import wollok.game.*
import ConfigGen.*
import texto.*

program juego {
	game.title("Bomberman del Subdesarrollo")
	game.cellSize(60)
	
	game.height(14)
	game.width(14)
	game.boardGround("map2.png")
	config.configuracionInicial()
	game.start()
}