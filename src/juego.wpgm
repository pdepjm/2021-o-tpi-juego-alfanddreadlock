import wollok.game.*
import ConfigGen.*
import texto.*

program juego {
	game.title("Bomberman del Subdesarrollo")
	game.cellSize(24)
	
	game.height(30)
	game.width(30)
	game.boardGround("map2.png")
	config.configuracionInicial()
	game.start()
}